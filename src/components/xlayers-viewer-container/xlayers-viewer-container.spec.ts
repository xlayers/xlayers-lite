import { XlayersViewerContainer } from './xlayers-viewer-container';

describe('xlayers-viewer-container', () => {
  it('builds', () => {
    expect(new XlayersViewerContainer()).toBeTruthy();
  });
});
